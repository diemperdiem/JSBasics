const interestRate = 0.3;
//No se peude reasignar el valor con const

let name = 'Gabriel'; //Strin Literal
let age = 30; //Number Literal
let isApproved = true; //Bool Literal
let firstName = undefined; //Not common but possible
let SelectedColor = null; //to clear

typeof name
name = 1
typeof name
                //las variables pueden cambiar de tipo (Dynamic Language)


console.log(name);